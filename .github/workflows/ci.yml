name: CI

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ "**" ]

jobs:
  build-test-lint:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          # Ensure optional deps for formatting/lint/test are present

      - name: Ruff Lint
        run: |
          python -m ruff check .

      - name: Ruff Format (diff only)
        run: |
          ruff format --check .

      - name: Black Format (diff only)
        run: |
          python -m black --check . || (
            echo '::error::Black formatting issues detected. Run "black ." locally.' && exit 1
          )

      - name: Type Check (mypy light)
        run: |
          python -m mypy --config-file mypy.ini src/core src/policy tests

      - name: Validate config (non-strict)
        run: |
          python scripts/validate_config_cli.py || true

      - name: Run PyTest
        env:
          PYTHONWARNINGS: always
        run: |
          python -m pytest -q --maxfail=1

      - name: Upload Ruff/pytest artifacts (on failure)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: logs
          path: |
            ./*.log
            **/pytest-*.xml
            **/.ruff_cache

  security:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install minimal deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pip-audit safety
      - name: Dependency Vulnerability Audit (pip-audit)
        run: |
          pip-audit -r requirements.txt || true
      - name: Safety Check
        run: |
          safety check -r requirements.txt || true
